name: aibuilderblog
static_sites:
  - name: aibuilderblog
    github:
      repo: YOUR_GITHUB_USERNAME/aibuilderblog.com
      branch: main
      deploy_on_push: true
    build_command: npm run build
    output_dir: _site
    environment_slug: node-js
    routes:
      - path: /
